-- create database biblioteca;
-- Use biblioteca;

-- create table autores(id int AUTO_INCREMENT primary key,
--  nombre varchar(100) not null ,
--  nacionalidad varchar(50) not null);

-- create table libro (isbn varchar(13)primary key,
-- titulo varchar(200)not null,
-- autor_id int,año_publicado int,
--  cantidad_total int not null,
--  cantidad_disponible int not null ,
--  foreign key (autor_id) references autores(id));

-- create table socios (id int auto_increment primary key ,
--  nombre varchar(200) not null,
--   apellido varchar(200) not null,
--   dni varchar(10) unique not null,
--   telefono varchar(15));

-- create table prestamos(
--     id int auto_increment primary key,
--     libro_isbn varchar(13),
--     socio_id int,
--     fecha_prestamo date not null,
--     fecha_devolucion_prevista date not null,
--     fecha_devolucion_real date null,
--     estado enum('prestado','devuelto') default 'prestado',
--     foreign key(libro_isbn) references libro(isbn),
--     foreign key(socio_id) references socios(id)
-- );
-- ALTER TABLE libro CHANGE `año_publicado` ano_publicado INT;